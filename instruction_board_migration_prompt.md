# 指示・発信ボード移植用プロンプト
以下の要件を満たす「指示・発信ボード」を、既存システムの技術構成・認証・デザイン規約に合わせて実装してください。
## 目的
管理者が災害情報を入力・発信し、発信履歴を確認・検索・再利用・訂正・削除できる画面を作る。
特に「掲示板掲載」は、将来ホーム画面のお知らせへ表示する公開データです。掲示板画面だけに閉じたデータにせず、ホーム画面が安全に取得・表示・絞り込みできるデータ構造とAPIを最初から用意してください。
## 実装前の確認
最初に既存コードを確認し、以下を再利用してください。
- 既存の認証・ログイン処理
- 既存の共通レイアウト、色、ボタン、フォーム部品
- 既存のデータベースまたはJSON保存方式
- 既存のマスターデータ取得方法
- 既存のホーム画面とお知らせ表示領域
- 既存の監査ログ方式
既存コードを壊す大規模な置き換えは避け、現在の設計に合わせて最小限の変更で実装してください。
## 画面構成
ページ名は「指示・発信ボード」とする。
発信種別を2つのタブで切り替える。
1. 情報送信（`INTERNAL`）
2. 掲示板掲載（`PUBLIC`）
### INTERNALの入力欄
- 災害種別
- ランク
- 地区・町村（複数選択）
- 交差点名、橋
- 発信団体
- 発信者名（自由入力）
- 位置情報
- 具体内容
### PUBLICの入力欄
- 誰向けか
- 地区・町村（複数選択）
- 地区・町村2（複数選択）
- 交差点名、橋
- 発信団体
- 発信者名（自由入力）
- 位置情報
- 具体内容
`INTERNAL` と `PUBLIC` で不要な入力欄は表示しない。
地区・町村は複数選択可能にし、保存時は配列または区切り文字で一貫した形式にする。読み込み時には複数の選択状態を正しく復元する。
## 必須入力チェック
`INTERNAL` の必須項目:
- 災害種別
- ランク
- 地区・町村
- 発信団体
- 具体内容
`PUBLIC` の必須項目:
- 誰向けか
- 地区・町村
- 発信団体
- 具体内容
サーバー側で必ず検証し、クライアント側の検証だけに依存しない。
## 位置情報
以下の機能を実装する。
- 緯度・経度の直接入力
- 現在地取得
- 地図表示・非表示
- 地図クリックによる位置選択
- 地名・住所検索
- 選択結果を緯度・経度として保存
外部地図サービスを使う場合は、既存のライブラリ・利用規約・API制限に従う。外部APIが失敗しても、通常の文字入力や送信処理は利用できるようにする。
## 保存データ構造
レポートは最低限、以下の項目を持つ。
```text
id
case_number
channel                 # INTERNAL または PUBLIC
disaster_type
rank
audience
district
district2
landmark
sender_org
sender_name
location
body
status                  # PUBLISHED / CORRECTED / REVOKED
parent_id               # 訂正元レポートのID。通常の新規発信は空
created_by
created_at
updated_at
```
将来のホーム画面で利用するため、`PUBLIC` データは `INTERNAL` と同じ履歴データベースに保存し、`channel` で明確に区別する。
タグ専用項目を追加する場合は、既存項目との重複を避ける。まずは以下の既存項目をタグとして利用できる構造にする。
- 災害種別
- ランク
- 誰向けか
- 地区・町村
- 地区・町村2
- 交差点名、橋
- 発信団体
## 履歴表示
入力フォームの下に履歴を表示する。
- 全履歴を表示する。直近2件などの件数制限を設けない
- 履歴領域には最大高さを設定し、縦スクロールできるようにする
- 各行に案件番号、状態、具体内容を表示する
- 具体内容だけでなく、各入力欄の値も表示する
- 未入力・未選択は「未入力」または「未選択」と表示する
- 地区・町村の複数選択値をすべて表示する
- `PUBLISHED` は公開中、`CORRECTED` は訂正済み、`REVOKED` は削除済みと表示する
- 訂正前後のレポートを案件番号で関連付けて表示する
  - 訂正前: `訂正版: <訂正版の案件番号>`
  - 訂正版: `訂正元: <訂正前の案件番号>`
- 表示専用の関連情報は元データを汚さず、表示時に解決する
## コピー機能
履歴の各行に小型の「コピー」ボタンを設置する。
コピー対象は具体内容だけではなく、保存されている全入力欄とする。
- 災害種別
- ランク
- 誰向けか
- 地区・町村
- 地区・町村2
- 交差点名、橋
- 発信団体
- 発信者名
- 位置情報
- 具体内容
- チャンネル
コピー時は現在の入力フォームへ値を復元する。
- select は選択値を復元する
- 地区・町村のチェックボックスは該当値をすべてチェックする
- マスターデータの読み込み前にコピーされた場合も、読み込み完了後に選択状態を再適用する
- コピーは古い履歴を含む全履歴で可能にする
- コピーしても、元レポートの状態や履歴データは変更しない
## 訂正機能
各履歴行から「訂正」を実行できるようにする。
- 対象レポートの全入力欄をフォームへ読み込む
- 管理者が全項目を編集できる
- 地区・町村の複数選択も編集できる
- 送信ボタンを「訂正内容を送信」に変更する
- 訂正は元レポートを直接上書きせず、新しいレポートを作成する
- 新しいレポートの `parent_id` に元レポートのIDを保存する
- 元レポートの状態を `CORRECTED` に変更する
- 訂正前と訂正版の両方を履歴に残す
- 監査ログに訂正操作、訂正版ID、訂正元IDを記録する
既存システムで公開中以外のレポートも訂正対象にする場合は、状態遷移が監査上安全か確認したうえで実装する。状態を無制限に変更して履歴の意味を壊さない。
## 削除機能
各履歴行から「削除」を実行できるようにする。
- 確認ダイアログを表示する
- 物理削除ではなく論理削除を基本とする
- `status` を `REVOKED` に変更する
- 削除済みの履歴も画面には残し、削除済みであることを表示する
- 監査ログに削除操作と対象IDを記録する
- ホームのお知らせには削除済みレポートを絶対に表示しない
## 履歴検索
履歴領域の上に検索UIを設置する。
### 自由文検索
案件番号、具体内容、災害種別、ランク、対象者、地区、場所、発信団体、発信者名、位置情報、状態など、履歴に表示可能な全項目を検索対象にする。
### 欄別タグ選択
以下の欄ごとに独立したタグ選択欄を用意する。
- 災害種別
- ランク
- 対象者
- 地区
- 場所
- 発信団体
タグ候補は表示中の履歴またはマスターデータから生成する。
複数のタグ欄を選択した場合は、すべての条件に一致する履歴だけを表示する（AND検索）。
### 検索実行タイミング
- 入力やタグ選択だけでは履歴を変更しない
- 「検索」ボタンを押した時だけ検索を実行する
- 自由文入力中の自動検索は行わない
- Enterキー対応は任意だが、検索ボタンと同じ処理を呼び出す
- 条件に一致する履歴だけ表示し、不一致の履歴は非表示にする
- 該当件数の表示は不要
- 検索結果が0件の場合は、履歴領域に「該当する履歴がありません」と表示する
- 検索条件をリセットする場合は、検索欄とタグ欄を空にして全件表示する
## API
管理者向けAPIと、ホーム画面向け公開APIを分ける。
### 管理者向け
以下の機能を用意する。
- レポート一覧取得
- レポート作成
- レポート詳細取得
- レポート全欄更新
- レポート訂正
- レポート論理削除
### ホーム画面向け公開API
掲示板掲載をホームのお知らせへ渡すため、ログイン不要で利用可能な公開取得経路を用意する。ただし、公開対象を厳密に制限する。
例:
```text
GET /api/public/announcements
```
必須条件:
- `channel == PUBLIC`
- `status == PUBLISHED`
- 削除済みを除外
- 訂正前のレポートを除外
- 訂正版を代表として返す
- 新しいものから返す
- ページングまたは最大件数を設ける
- 必要に応じて地区、災害種別、ランク、対象者、発信団体で絞り込める
- ホーム画面へ返すデータに管理者専用情報や監査情報を含めない
- 管理者APIの認証を公開APIに流用しない
訂正済みレポートを除外するだけではなく、`parent_id` をたどって最新版の公開レポートを決定する。訂正の連鎖が発生する場合は、最終的な訂正版を代表として返す。
公開APIのレスポンス例:
```json
{
  "items": [
    {
      "id": 12,
      "case_number": "2026-0820-0001",
      "audience": "住民",
      "disaster_type": "大雨・洪水",
      "rank": "警戒",
      "district": "青森市全域",
      "district2": "",
      "landmark": "青森駅前",
      "sender_org": "青森市役所",
      "sender_name": "防災担当",
      "location": "40.824600,140.740000",
      "body": "避難に関するお知らせ",
      "published_at": "2026年08月20日 10:00",
      "tags": ["大雨・洪水", "警戒", "住民", "青森市全域"]
    }
  ],
  "total": 1
}
```
## ホーム画面への連携
ホーム画面に「お知らせ」領域を用意し、ページ読み込み時に公開APIから掲示板掲載を取得する。
- 掲示板画面の履歴を直接参照しない
- ホーム画面は公開APIを利用する
- `PUBLIC` だけを表示する
- `PUBLISHED` だけを表示する
- 訂正済み・削除済み・訂正前は表示しない
- お知らせには本文、災害種別、ランク、対象者、地区、発信団体、掲載日時を表示する
- タグを表示する
- タグ選択による絞り込みを可能にする
- 日時順、災害種別順、ランク順、地区順などの並び替えを可能にする
- API失敗時は画面全体を壊さず、取得失敗メッセージを表示する
- 公開APIに認証情報や管理者向け操作ボタンを出さない
ホーム画面での表示例:
```text
お知らせ
[災害種別] [ランク] [対象者] [地区] [並び順]
[警戒] 大雨・洪水
青森市全域 / 住民
青森市役所
避難に関するお知らせ
掲載日時: 2026年08月20日 10:00
```
## 状態・整合性
以下を必ず守る。
- 作成、訂正、削除の保存失敗時はメモリ上の変更も元に戻す
- 操作成功後に監査ログを保存する
- 訂正元と訂正版を案件番号・IDで追跡できる
- ホーム画面には公開可能な情報だけを返す
- HTMLへ値を埋め込む場合は適切にエスケープする
- JSONをHTML属性へ埋め込む場合は安全にエンコードする
- APIの入力値を必ずサーバー側で検証する
- 同じレポートを二重送信しにくいUIにする
- 画面幅が狭い場合も、入力欄・検索欄・履歴操作が重ならないようにする
## 受け入れ条件
実装後、以下をすべて確認する。
1. INTERNALとPUBLICを切り替えられる
2. 必須項目を満たさない発信が保存されない
3. 地区を複数選択して保存・コピー・訂正できる
4. 位置情報を入力・地図選択・検索できる
5. 全履歴が表示され、履歴領域を縦スクロールできる
6. 全履歴行で全欄コピーができる
7. 全履歴行で訂正と削除ができる
8. 訂正前と訂正版が相互に関連表示される
9. 削除済みが履歴に残り、状態が表示される
10. 検索ボタンを押した時だけ自由文検索が実行される
11. 欄別タグを複数指定してAND検索できる
12. 該当しない履歴が表示から消える
13. 検索結果0件を適切に表示する
14. PUBLICの公開APIがPUBLISHEDの最新版だけを返す
15. 削除済み・訂正前・管理者専用情報がホームへ渡らない
16. ホーム画面でPUBLICのお知らせを表示できる
17. ホーム画面でタグ絞り込みと並び替えができる
18. PCとスマートフォンで表示が崩れない
19. 保存失敗時にデータ不整合が起きない
20. 自動テストまたはテストクライアントで主要機能を検証する
## 実装時の進め方
1. 既存のルート、モデル、テンプレート、ホーム画面を確認する
2. データ構造と公開APIの責務を先に決める
3. 管理者向け入力・保存・履歴を実装する
4. コピー・訂正・削除を実装する
5. 履歴検索と欄別タグを実装する
6. PUBLIC公開APIを実装する
7. ホームのお知らせ表示、タグ絞り込み、並び替えを実装する
8. 主要機能をテストする
9. 変更ファイル、確認したテスト、既知の制約を報告する
最後に、実装したファイル、API仕様、データ状態の扱い、テスト結果を簡潔に説明してください。
